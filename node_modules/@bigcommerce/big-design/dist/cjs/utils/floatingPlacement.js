"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getFloatingPlacement = getFloatingPlacement;
var _react = require("@floating-ui/react");
// Placement props are still typed with popper's Placement, which includes 'auto*'
// values that floating-ui only supports via the autoPlacement middleware.
function getFloatingPlacement(placement) {
  switch (placement) {
    case 'auto':
      return {
        middleware: (0, _react.autoPlacement)()
      };
    case 'auto-start':
      return {
        middleware: (0, _react.autoPlacement)({
          alignment: 'start'
        })
      };
    case 'auto-end':
      return {
        middleware: (0, _react.autoPlacement)({
          alignment: 'end'
        })
      };
    default:
      return {
        middleware: (0, _react.flip)(),
        placement
      };
  }
}
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useFocusable = void 0;
var _react = require("react");
// prettier-ignore
const useFocusable = ({
  nodes,
  type,
  defaultSelected
}) => {
  /*
   * We purposefully want to focus on the first node if defaultSelected is empty,
   * with the assumption that the tree is fully collapsed.
   */
  const [focusedNode, setFocusedNode] = (0, _react.useState)(type && defaultSelected?.length ? defaultSelected[0] : nodes[0].id);
  const onFocus = (0, _react.useCallback)(nodeId => {
    setFocusedNode(nodeId);
  }, []);
  return {
    focusedNode,
    onFocus
  };
};
exports.useFocusable = useFocusable;
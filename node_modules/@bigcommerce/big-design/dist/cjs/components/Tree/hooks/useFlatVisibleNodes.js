"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useFlatVisibleNodes = void 0;
var _react = require("react");
// prettier-ignore
const flatten = (nodes, expanded, depth, acc) => {
  const setsize = nodes.length;
  nodes.forEach((node, index) => {
    acc.push({
      node,
      depth,
      posinset: index + 1,
      setsize
    });
    if (node.children?.length && expanded.has(node.id)) {
      flatten(node.children, expanded, depth + 1, acc);
    }
  });
  return acc;
};

// prettier-ignore
const useFlatVisibleNodes = ({
  nodes,
  expandedNodes
}) => (0, _react.useMemo)(() => flatten(nodes, expandedNodes, 0, []), [nodes, expandedNodes]);
exports.useFlatVisibleNodes = useFlatVisibleNodes;
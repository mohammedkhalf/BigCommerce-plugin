"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.validateFiles = exports.validateFileFormat = void 0;
const validateFileFormat = (file, fileFormats = '') => {
  const allowedFormats = fileFormats.split(',').map(format => format.trim());
  const fileExtension = file.type.split('/').pop()?.toLowerCase() ?? '';
  return allowedFormats.some(format => {
    if (format === '*' || format === '.*') {
      return true;
    } else if (format.startsWith('.')) {
      return format === `.${fileExtension}`;
    } else if (format.startsWith('audio/') || format.startsWith('video/') || format.startsWith('image/')) {
      const mediaTypePrefix = format.split('/')[0];
      return file.type.startsWith(`${mediaTypePrefix}/`);
    }
    return true;
  });
};
exports.validateFileFormat = validateFileFormat;
const validateFiles = (files, validators) => files.flatMap((file, idx) => validators.filter(({
  validator
}) => !validator(file)).map(({
  message,
  type
}) => ({
  file,
  fileIdx: idx,
  message,
  type
})));
exports.validateFiles = validateFiles;
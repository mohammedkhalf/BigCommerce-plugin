"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.loadImagePreview = exports.getImagesPreview = void 0;
const loadImagePreview = file => {
  if (!file.type.startsWith('image/')) {
    return Promise.resolve(null);
  }
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onload = event => {
      // eslint-disable-next-line @typescript-eslint/consistent-type-assertions
      const imageUrl = event.target?.result;
      resolve(imageUrl);
    };
    reader.onerror = error => {
      // eslint-disable-next-line @typescript-eslint/prefer-promise-reject-errors
      reject(error);
    };
    reader.readAsDataURL(file);
  });
};
exports.loadImagePreview = loadImagePreview;
const getImagesPreview = async files => files.reduce(async (prevPromise, file, idx) => {
  const prevResult = await prevPromise;
  const imageUrl = await loadImagePreview(file);
  return {
    ...prevResult,
    [idx]: imageUrl
  };
}, Promise.resolve({}));
exports.getImagesPreview = getImagesPreview;
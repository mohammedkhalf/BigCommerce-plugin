"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.FileUploader = void 0;
var _bigDesignIcons = require("@bigcommerce/big-design-icons");
var _react = _interopRequireWildcard(require("react"));
var _utils = require("../../utils");
var _Form = require("../Form");
var _useInputErrors = require("../Form/useInputErrors");
var _InlineMessage = require("../InlineMessage");
var _constants = require("./constants");
var _File = require("./File");
var _styled = require("./styled");
var _utils2 = require("./utils");
var _jsxRuntime = require("react/jsx-runtime");
const FileUploader = ({
  accept,
  actions,
  description,
  disabled,
  dropzoneConfig = {},
  error,
  files,
  label,
  labelId,
  localization = _constants.defaultLocalization,
  multiple,
  previewHidden = false,
  validators = [],
  onFilesChange,
  onFilesError,
  ...props
}) => {
  const [imagePreview, setImagePreview] = (0, _react.useState)({});
  const [isAllErrorsVisible, setIsAllErrorsVisible] = (0, _react.useState)(false);
  const prevErrors = (0, _react.useRef)([]);
  const uniqueInputId = (0, _react.useId)();
  const id = props.id ?? uniqueInputId;
  const validationErrors = (0, _react.useMemo)(() => {
    const flatFilesErrors = (0, _utils2.validateFiles)(files, validators);
    return flatFilesErrors.reduce((acc, {
      file,
      fileIdx,
      message,
      type
    }) => {
      const existingError = acc.find(error => error.fileIdx === fileIdx);
      if (!existingError) {
        return [...acc, {
          file,
          fileIdx,
          message,
          type
        }];
      }
      existingError.type = Array.isArray(existingError.type) ? [...existingError.type, type] : [existingError.type, type];
      existingError.message = existingError.message ? `${existingError.message}, ${message}` : message;
      return acc;
    }, []);
  }, [files, validators]);
  (0, _useInputErrors.useInputErrors)(id, multiple ? undefined : error);
  (0, _react.useEffect)(() => {
    (0, _utils2.getImagesPreview)(files).then(previews => {
      setImagePreview(previews);
    });
  }, [files]);
  (0, _react.useEffect)(() => {
    if (onFilesError && (validationErrors.length > 0 || prevErrors.current.length > 0 && validationErrors.length === 0)) {
      onFilesError(validationErrors);
      prevErrors.current = validationErrors;
    }
  }, [onFilesError, validationErrors]);
  const handleFilesChange = (0, _react.useCallback)(newFiles => {
    const filesList = Array.from(newFiles || []);
    if (!multiple) {
      onFilesChange([filesList[0]]);
      return;
    }
    onFilesChange([...files, ...filesList]);
  }, [files, multiple, onFilesChange]);
  const handleFileRemove = (0, _react.useCallback)((removedName, removedIdx) => {
    const filteredFiles = files.filter(({
      name
    }, idx) => !(removedName === name && removedIdx === idx));
    onFilesChange(filteredFiles);
  }, [files, onFilesChange]);
  const renderedLabel = (0, _react.useMemo)(() => {
    if (!label) {
      return null;
    }
    if (typeof label === 'string') {
      return /*#__PURE__*/(0, _jsxRuntime.jsx)(_Form.FormControlLabel, {
        htmlFor: id,
        required: props.required,
        children: label
      });
    }
    if (/*#__PURE__*/(0, _react.isValidElement)(label) && label.type === _Form.FormControlLabel) {
      return /*#__PURE__*/(0, _react.cloneElement)(label, {
        id: labelId,
        htmlFor: id
      });
    }
    (0, _utils.warning)('label must be either a string or a FormControlLabel component.');
  }, [id, label, labelId, props.required]);
  const renderedDescription = (0, _react.useMemo)(() => {
    if (!description) {
      return null;
    }
    if (typeof description === 'string') {
      return /*#__PURE__*/(0, _jsxRuntime.jsx)(_Form.FormControlDescription, {
        children: description
      });
    }
    if (/*#__PURE__*/(0, _react.isValidElement)(description) && description.type === _Form.FormControlDescription) {
      return description;
    }
    (0, _utils.warning)('description must be either a string or a FormControlDescription component.');
  }, [description]);
  const handleToggleErrors = (0, _react.useCallback)(event => {
    event.preventDefault();
    setIsAllErrorsVisible(prev => !prev);
  }, []);
  const renderedErrors = (0, _react.useMemo)(() => {
    if (!multiple || !error || error.length === 0) {
      return null;
    }
    const isErrorsArray = Array.isArray(error);
    const messages = isErrorsArray ? error.map(text => ({
      text
    })) : [{
      text: error
    }];
    const firstMessages = messages.slice(0, 10);
    const restMessages = messages.slice(10);
    return /*#__PURE__*/(0, _jsxRuntime.jsx)(_InlineMessage.InlineMessage, {
      actions: restMessages.length > 0 ? [{
        iconRight: isAllErrorsVisible ? /*#__PURE__*/(0, _jsxRuntime.jsx)(_bigDesignIcons.ExpandLessIcon, {}) : /*#__PURE__*/(0, _jsxRuntime.jsx)(_bigDesignIcons.ExpandMoreIcon, {}),
        text: isAllErrorsVisible ? localization.showLess : localization.showMore,
        variant: 'subtle',
        onClick: handleToggleErrors
      }] : undefined,
      header: localization.canNotUploadTheseFiles,
      marginVertical: "large",
      messages: [...firstMessages, ...(isAllErrorsVisible ? restMessages : [])],
      type: "error"
    });
  }, [error, handleToggleErrors, isAllErrorsVisible, localization.canNotUploadTheseFiles, localization.showLess, localization.showMore, multiple]);
  const getActions = (0, _react.useCallback)((file, idx) => actions?.map(({
    onItemClick,
    ...action
  }) => ({
    ...action,
    onItemClick: () => onItemClick(file, idx)
  })), [actions]);
  const renderedFiles = (0, _react.useMemo)(() => {
    if (previewHidden) {
      return null;
    }
    if (!multiple && files.length > 0) {
      const file = files[0];
      const isFileInvalid = validationErrors.some(({
        fileIdx
      }) => fileIdx === 0);
      const imageSrc = imagePreview[0];
      return /*#__PURE__*/(0, _jsxRuntime.jsx)(_File.File, {
        actions: getActions(files[0], 0),
        idx: 0,
        isValid: !isFileInvalid,
        name: file.name,
        onRemove: name => handleFileRemove(name, 0),
        previewSrc: imageSrc
      });
    }
    const filesList = files.map((file, idx) => {
      const isFileInvalid = validationErrors.some(({
        fileIdx
      }) => fileIdx === idx);
      const imageSrc = imagePreview[idx];
      return /*#__PURE__*/(0, _jsxRuntime.jsx)("li", {
        children: /*#__PURE__*/(0, _jsxRuntime.jsx)(_File.File, {
          actions: getActions(file, idx),
          idx: idx,
          isValid: !isFileInvalid,
          name: file.name,
          onRemove: name => handleFileRemove(name, idx),
          previewSrc: imageSrc
        })
      }, idx);
    });
    return /*#__PURE__*/(0, _jsxRuntime.jsx)(_styled.StyledList, {
      children: filesList
    });
  }, [previewHidden, multiple, files, validationErrors, imagePreview, getActions, handleFileRemove]);
  const renderedDropzone = (0, _react.useMemo)(() => {
    if (!multiple && files.length > 0) {
      return null;
    }
    const minBlockHeight = 180;
    const viewType = !files.length && (dropzoneConfig.emptyHeight ?? 0) >= minBlockHeight ? 'block' : 'row';
    return /*#__PURE__*/(0, _jsxRuntime.jsx)(_styled.StyledDropZoneWrapper, {
      accept: accept,
      action: dropzoneConfig.action,
      description: dropzoneConfig.description,
      disabled: disabled,
      emptyHeight: files.length ? undefined : dropzoneConfig.emptyHeight,
      icon: dropzoneConfig.icon,
      id: id,
      label: dropzoneConfig.label,
      localization: localization,
      multiple: multiple,
      onFilesChange: handleFilesChange,
      viewType: viewType
    });
  }, [accept, dropzoneConfig.action, disabled, dropzoneConfig.description, dropzoneConfig.emptyHeight, dropzoneConfig.icon, dropzoneConfig.label, files.length, handleFilesChange, id, localization, multiple]);
  return /*#__PURE__*/(0, _jsxRuntime.jsxs)("div", {
    children: [renderedLabel, renderedDescription, /*#__PURE__*/(0, _jsxRuntime.jsxs)(_styled.StyledFileUploaderWrapper, {
      children: [renderedFiles, renderedDropzone]
    }), renderedErrors]
  });
};
exports.FileUploader = FileUploader;
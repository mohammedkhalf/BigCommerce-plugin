"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.AnchorNav = void 0;
var _react = _interopRequireWildcard(require("react"));
var _reactIntersectionObserver = require("react-intersection-observer");
var _Box = require("../Box");
var _Flex = require("../Flex");
var _styled = require("./styled");
var _jsxRuntime = require("react/jsx-runtime");
const useAnchorObserver = (id, setActiveId, suspendObserverRef) => {
  const {
    ref,
    inView
  } = (0, _reactIntersectionObserver.useInView)({
    threshold: 0.1,
    rootMargin: '0px 0px -90% 0px'
  });
  (0, _react.useEffect)(() => {
    if (inView && !suspendObserverRef.current) {
      setActiveId(id);
    }
  }, [inView, id, setActiveId, suspendObserverRef]);
  return ref;
};
const AnchorSection = ({
  id,
  children,
  setActiveId,
  suspendObserverRef,
  registerSection
}) => {
  // We now know that observerRef is a function callback.
  const observerRef = useAnchorObserver(id, setActiveId, suspendObserverRef);

  // Combine the observer ref with the registration of the section.
  const combinedRef = (0, _react.useCallback)(el => {
    registerSection(id, el);
    observerRef(el); // Always call the ref function directly.
  }, [id, registerSection, observerRef]);
  return /*#__PURE__*/(0, _jsxRuntime.jsx)(_Flex.FlexItem, {
    id: id,
    ref: combinedRef,
    style: {
      scrollMarginTop: '80px'
    },
    children: children
  });
};
const AnchorNav = ({
  elements
}) => {
  const [activeId, setActiveId] = (0, _react.useState)(null);
  const sectionRefs = (0, _react.useRef)({});
  const suspendObserverRef = (0, _react.useRef)(false);
  const resumeTimeoutRef = (0, _react.useRef)(null);
  const updateUrlHash = (0, _react.useCallback)(id => {
    const newHash = `#${id}`;
    if (window.location.hash !== newHash) {
      history.replaceState(null, '', newHash); // Avoids adding to browser history
    }
  }, []);
  const scrollToSection = (0, _react.useCallback)(id => {
    const el = sectionRefs.current[id];
    if (el) {
      el.scrollIntoView({
        behavior: 'smooth'
      });
      setActiveId(id);
      updateUrlHash(id);
      suspendObserverRef.current = true;
      if (resumeTimeoutRef.current) {
        clearTimeout(resumeTimeoutRef.current);
      }
      resumeTimeoutRef.current = setTimeout(() => {
        suspendObserverRef.current = false;
      }, 1000);
    }
  }, [updateUrlHash]);

  // Scroll to section based on the URL hash when the component loads.
  (0, _react.useEffect)(() => {
    const hash = window.location.hash?.slice(1);
    if (hash && sectionRefs.current[hash]) {
      scrollToSection(hash);
    }
  }, [scrollToSection]);

  // Cleanup timeout on unmount.
  (0, _react.useEffect)(() => {
    return () => {
      if (resumeTimeoutRef.current) {
        clearTimeout(resumeTimeoutRef.current);
      }
    };
  }, []);
  return /*#__PURE__*/(0, _jsxRuntime.jsxs)(_Box.Box, {
    children: [/*#__PURE__*/(0, _jsxRuntime.jsx)(_styled.StyledAnchorNavList, {
      children: /*#__PURE__*/(0, _jsxRuntime.jsx)("ul", {
        children: elements.map(({
          id,
          label
        }) => /*#__PURE__*/(0, _jsxRuntime.jsx)("li", {
          children: /*#__PURE__*/(0, _jsxRuntime.jsx)("a", {
            "aria-current": id === activeId ? 'true' : undefined,
            className: id === activeId ? 'active' : '',
            href: `#${id}`,
            onClick: e => {
              e.preventDefault();
              scrollToSection(id);
            },
            children: label
          })
        }, id))
      })
    }), /*#__PURE__*/(0, _jsxRuntime.jsx)(_Flex.Flex, {
      flexDirection: "column",
      flexGap: "xLarge",
      children: elements.map(({
        id,
        element
      }) => /*#__PURE__*/(0, _jsxRuntime.jsx)(AnchorSection, {
        id: id,
        registerSection: (id, el) => {
          sectionRefs.current[id] = el;
        },
        setActiveId: setActiveId,
        suspendObserverRef: suspendObserverRef,
        children: element
      }, id))
    })]
  });
};
exports.AnchorNav = AnchorNav;
AnchorNav.displayName = 'AnchorNav';
"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Grid = void 0;
var _react = _interopRequireWildcard(require("react"));
var _styled = require("./styled");
var _withGrid = require("./withGrid");
var _jsxRuntime = require("react/jsx-runtime");
// `display` is passed twice deliberately: StyledGrid composes over Box (a component, not
// a tag), so `display` also needs to reach Box's own internal $-prefixed handling
// untouched (its public contract), while `$display` feeds StyledGrid's own separate
// withDisplay() call on its outer layer.
const RawGrid = props => {
  const {
    as,
    display,
    forwardedRef,
    gridAreas,
    gridAutoColumns,
    gridAutoFlow,
    gridAutoRows,
    gridColumns,
    gridColumnGap,
    gridGap,
    gridRows,
    gridRowGap,
    gridTemplate,
    ...domProps
  } = props;
  return /*#__PURE__*/(0, _jsxRuntime.jsx)(_styled.StyledGrid, {
    $display: display,
    display: display,
    forwardedAs: as,
    ref: forwardedRef,
    ...domProps,
    ...(0, _withGrid.toTransientGridedContainerProps)(props)
  });
};
const Grid = exports.Grid = /*#__PURE__*/(0, _react.forwardRef)((props, ref) => /*#__PURE__*/(0, _jsxRuntime.jsx)(RawGrid, {
  ...props,
  forwardedRef: ref
}));
"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.StylableCounter = exports.Counter = void 0;
var _bigDesignIcons = require("@bigcommerce/big-design-icons");
var _react = _interopRequireWildcard(require("react"));
var _utils = require("../../utils");
var _Form = require("../Form");
var _useInputErrors = require("../Form/useInputErrors");
var _styled = require("./styled");
var _jsxRuntime = require("react/jsx-runtime");
const defaultLocalization = {
  decreaseCount: 'Decrease count',
  increaseCount: 'Increase count'
};
const StylableCounter = exports.StylableCounter = (0, _utils.typedMemo)(({
  min = 0,
  max = 100,
  step = 1,
  forwardedRef,
  label,
  labelId,
  localization = defaultLocalization,
  description,
  error,
  disabled,
  value,
  onCountChange,
  ...props
}) => {
  const [focus, setFocus] = (0, _react.useState)(false);
  const uniqueCounterId = (0, _react.useId)();
  const id = props.id ? props.id : uniqueCounterId;
  const {
    errors
  } = (0, _useInputErrors.useInputErrors)(id, error);
  (0, _react.useEffect)(() => {
    if (!Number.isInteger(value)) {
      onCountChange(Math.round(value));
    }
  }, [onCountChange, value]);
  const handleFocus = event => {
    const {
      onFocus
    } = props;
    setFocus(true);
    if (onFocus) {
      onFocus(event);
    }
  };
  const handleBlur = event => {
    const {
      onBlur
    } = props;
    setFocus(false);
    if (onBlur) {
      onBlur(event);
    }
  };
  const handleIncrease = () => {
    if (value + step > Number(max)) {
      return;
    }

    // Checks that the provided value is a multiple of the step
    if (value % step === 0) {
      onCountChange(value + step);
    }
    // If not, returns nearest higher value that is a multiple of the step
    else {
      onCountChange(value + (step - value % step));
    }
  };
  const handleDecrease = () => {
    if (value - step < Number(min)) {
      return;
    }

    // Checks that the provided value is a multiple of the step
    if (value % step === 0) {
      onCountChange(value - step);
    }
    // If not, returns nearest lower value that is a multiple of the step
    else {
      onCountChange(value - value % step);
    }
  };
  const handleChange = event => {
    const newValue = Number(event.currentTarget.value);
    if (isNaN(newValue)) {
      return;
    }
    if (!Number.isInteger(newValue)) {
      onCountChange(Math.round(newValue));
    }
    if (newValue >= Number(min) && newValue <= Number(max)) {
      onCountChange(newValue);
    }
  };
  const handleKeyPress = event => {
    switch (event.key) {
      case 'ArrowUp':
        handleIncrease();
        break;
      case 'ArrowDown':
        handleDecrease();
        break;
      case 'Escape':
        onCountChange(0);
        break;
      default:
        break;
    }
  };
  const renderedLabel = (0, _react.useMemo)(() => {
    if (!label) {
      return null;
    }
    if (typeof label === 'string') {
      return /*#__PURE__*/(0, _jsxRuntime.jsx)(_Form.FormControlLabel, {
        htmlFor: id,
        id: labelId,
        required: props.required,
        children: label
      });
    }
    if (/*#__PURE__*/(0, _react.isValidElement)(label) && label.type === _Form.FormControlLabel) {
      return /*#__PURE__*/(0, _react.cloneElement)(label, {
        id: labelId,
        htmlFor: id
      });
    }
    (0, _utils.warning)('label must be either a string or a FormControlLabel component.');
  }, [id, label, labelId, props.required]);
  const renderedDescription = (0, _react.useMemo)(() => {
    if (!description) {
      return null;
    }
    if (typeof description === 'string') {
      return /*#__PURE__*/(0, _jsxRuntime.jsx)(_Form.FormControlDescription, {
        children: description
      });
    }
    if (/*#__PURE__*/(0, _react.isValidElement)(description) && description.type === _Form.FormControlDescription) {
      return description;
    }
    (0, _utils.warning)('description must be either a string or a FormControlDescription component.');
  }, [description]);
  return /*#__PURE__*/(0, _jsxRuntime.jsxs)("div", {
    children: [renderedLabel, renderedDescription, /*#__PURE__*/(0, _jsxRuntime.jsxs)(_styled.StyledCounterWrapper, {
      $error: errors,
      $focus: focus,
      disabled: disabled,
      children: [/*#__PURE__*/(0, _jsxRuntime.jsx)(_styled.StyledCounterButton, {
        disabled: disabled || value <= Number(min),
        iconOnly: /*#__PURE__*/(0, _jsxRuntime.jsx)(_bigDesignIcons.RemoveCircleOutlineIcon, {
          title: localization.decreaseCount
        }),
        onClick: handleDecrease,
        type: "button"
      }), /*#__PURE__*/(0, _jsxRuntime.jsx)(_styled.StyledCounterInput, {
        ...props,
        disabled: disabled,
        id: id,
        onBlur: handleBlur,
        onChange: handleChange,
        onFocus: handleFocus,
        onKeyDown: handleKeyPress,
        ref: forwardedRef,
        value: value
      }), /*#__PURE__*/(0, _jsxRuntime.jsx)(_styled.StyledCounterButton, {
        disabled: disabled || value >= Number(max),
        iconOnly: /*#__PURE__*/(0, _jsxRuntime.jsx)(_bigDesignIcons.AddCircleOutlineIcon, {
          title: localization.increaseCount
        }),
        onClick: handleIncrease,
        type: "button"
      })]
    })]
  });
});
const Counter = exports.Counter = (0, _utils.typedMemo)(/*#__PURE__*/(0, _react.forwardRef)(({
  className,
  style,
  ...props
}, ref) => /*#__PURE__*/(0, _jsxRuntime.jsx)(StylableCounter, {
  ...props,
  forwardedRef: ref
})));
"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.UpdateItemsProvider = exports.UpdateItemsContext = void 0;
var _react = _interopRequireWildcard(require("react"));
var _shallow = require("zustand/shallow");
var _utils = require("../../../../utils");
var _hooks = require("../../hooks");
var _jsxRuntime = require("react/jsx-runtime");
const UpdateItemsContext = exports.UpdateItemsContext = /*#__PURE__*/(0, _react.createContext)(null);
const UpdateItemsProvider = exports.UpdateItemsProvider = (0, _utils.typedMemo)(({
  children,
  items
}) => {
  const {
    store,
    useStore
  } = (0, _hooks.useWorksheetStore)();
  const setRows = useStore(store, (0, _shallow.useShallow)(state => state.setRows));
  const addEditedCells = useStore(store, (0, _shallow.useShallow)(state => state.addEditedCells));
  const updateItems = (0, _react.useCallback)((cells, newValues) => {
    setRows(cells.reduce((accum, cell, index) => {
      const {
        hash,
        rowIndex
      } = cell;

      // Don't change since value is the same
      if (cell.value === newValues[index]) {
        return accum;
      }
      const row = accum[rowIndex];
      const updatedRow = {
        ...row,
        [hash]: newValues[index]
      };
      accum[rowIndex] = updatedRow;
      return accum;
    }, items));
    addEditedCells(cells.reduce((accum, cell, index) => {
      // Don't add since value is the same
      if (cell.value === newValues[index]) {
        return accum;
      }
      return [...accum, {
        ...cell,
        value: newValues[index]
      }];
    }, []));
  }, [addEditedCells, items, setRows]);
  const providerValue = (0, _react.useMemo)(() => ({
    updateItems
  }), [updateItems]);
  return /*#__PURE__*/(0, _jsxRuntime.jsx)(UpdateItemsContext.Provider, {
    value: providerValue,
    children: children
  });
});
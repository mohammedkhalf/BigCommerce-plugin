"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useKeyEvents = void 0;
var _react = require("react");
var _shallow = require("zustand/shallow");
var _useNavigation = require("../useNavigation");
var _useWorksheetStore = require("../useWorksheetStore");
const useKeyEvents = () => {
  const {
    store,
    useStore
  } = (0, _useWorksheetStore.useWorksheetStore)();

  // Get the first cell of the selected values
  const selectedCell = useStore(store, (0, _shallow.useShallow)(state => state.selectedCells[0]));
  const lastSelectedCell = useStore(store, (0, _shallow.useShallow)(state => state.selectedCells[state.selectedCells.length - 1]));
  const selectedCells = useStore(store, (0, _shallow.useShallow)(state => state.selectedCells));
  const rows = useStore(store, (0, _shallow.useShallow)(state => state.rows));
  const isEditing = useStore(store, (0, _shallow.useShallow)(state => state.editingCell !== null));
  const isShiftPressed = useStore(store, (0, _shallow.useShallow)(state => state.isShiftPressed));
  const setShiftPressed = useStore(store, (0, _shallow.useShallow)(state => state.setShiftPressed));
  const setEditingCell = useStore(store, (0, _shallow.useShallow)(state => state.setEditingCell));
  const {
    navigate
  } = (0, _useNavigation.useNavigation)(selectedCell);
  const setSelectedCells = useStore(store, (0, _shallow.useShallow)(state => state.setSelectedCells));
  const editSelectedCell = (0, _react.useCallback)(({
    isMetaKey = false,
    isControlKey = false,
    editWithValue = ''
  } = {}) => {
    if (selectedCell) {
      setEditingCell({
        cell: selectedCell,
        isMetaKey,
        isControlKey,
        editWithValue
      });
    }
  }, [selectedCell, setEditingCell]);
  const handleKeyUp = (0, _react.useCallback)(event => {
    const key = event.key;
    if (key === 'Shift') {
      setShiftPressed(false);
    }
  }, [setShiftPressed]);
  const handleKeyDown = (0, _react.useCallback)(event => {
    const key = event.key;
    if (event.metaKey || event.key === 'c' && (event.ctrlKey || event.metaKey)) {
      return;
    }
    if (isEditing) {
      switch (key) {
        case 'Enter':
          navigate({
            rowIndex: 1,
            columnIndex: 0
          });
          break;
        case 'Tab':
          navigate({
            rowIndex: 0,
            columnIndex: event.shiftKey ? -1 : 1
          });
          break;
      }
    } else {
      switch (key) {
        case 'Enter':
          if (selectedCell && !selectedCell.disabled) {
            editSelectedCell({});
            if (selectedCell?.type === 'checkbox') {
              navigate({
                rowIndex: 1,
                columnIndex: 0
              });
            }
          }
          break;
        case ' ':
          if (selectedCell && !selectedCell.disabled) {
            editSelectedCell();
          }
          break;
        case 'ArrowUp':
          if (isShiftPressed && selectedCells?.length > 1) {
            setSelectedCells(selectedCells.slice(0, -1));
            break;
          }
          navigate({
            rowIndex: -1,
            columnIndex: 0
          });
          break;
        case 'ArrowDown':
          if (isShiftPressed) {
            const nextRowIdx = lastSelectedCell.rowIndex + 1;
            const nextRow = rows[nextRowIdx];
            if (nextRowIdx <= rows.length - 1) {
              setSelectedCells([...selectedCells, {
                ...lastSelectedCell,
                rowIndex: lastSelectedCell.rowIndex + 1,
                value: nextRow[lastSelectedCell.hash]
              }]);
            }
          } else {
            navigate({
              rowIndex: 1,
              columnIndex: 0
            });
          }
          break;
        case 'ArrowRight':
          navigate({
            rowIndex: 0,
            columnIndex: 1
          });
          break;
        case 'Tab':
          navigate({
            rowIndex: 0,
            columnIndex: event.shiftKey ? -1 : 1
          });
          break;
        case 'ArrowLeft':
          navigate({
            rowIndex: 0,
            columnIndex: -1
          });
          break;
        case 'Meta':
          if (selectedCell) {
            editSelectedCell({
              isMetaKey: true
            });
          }
          break;
        case 'Control':
          if (selectedCell) {
            editSelectedCell({
              isControlKey: true
            });
          }
          break;
        case 'Shift':
          if (selectedCell) {
            setShiftPressed(true);
          }
          break;
        default:
          if (key !== 'Escape' && key.length === 1 && (selectedCell?.type === 'text' || selectedCell?.type === 'number')) {
            event.preventDefault();
            editSelectedCell({
              editWithValue: key
            });
          }
          break;
      }
      event.preventDefault();
    }
  }, [isEditing, navigate, selectedCell, isShiftPressed, selectedCells, editSelectedCell, setSelectedCells, lastSelectedCell, rows, setShiftPressed]);
  return (0, _react.useMemo)(() => ({
    handleKeyDown,
    handleKeyUp
  }), [handleKeyDown, handleKeyUp]);
};
exports.useKeyEvents = useKeyEvents;
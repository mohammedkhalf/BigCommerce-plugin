"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Cell = void 0;
var _react = _interopRequireWildcard(require("react"));
var _shallow = require("zustand/shallow");
var _utils = require("../../../utils");
var _Tooltip = require("../../Tooltip");
var _Typography = require("../../Typography");
var _editors = require("../editors");
var _MultiSelectEditor = require("../editors/MultiSelectEditor");
var _hooks = require("../hooks");
var _utils2 = require("../utils");
var _styled = require("./styled");
var _jsxRuntime = require("react/jsx-runtime");
const InternalCell = ({
  columnIndex,
  disabled = false,
  formatting,
  hash,
  options,
  rowIndex,
  type,
  rowId,
  validation,
  notation,
  value,
  nextRowValue,
  isChild,
  isLastChild,
  action
}) => {
  const cell = (0, _react.useMemo)(() => ({
    columnIndex,
    disabled,
    hash,
    rowIndex,
    type,
    value
  }), [columnIndex, disabled, hash, rowIndex, type, value]);
  const cellIdx = (0, _react.useMemo)(() => (0, _utils2.getCellIdx)(cell), [cell]);
  const {
    handleBlur,
    handleChange,
    handleDoubleClick,
    handleKeyDown,
    isEditing
  } = (0, _hooks.useEditableCell)(cell);
  const {
    store,
    useStore
  } = (0, _hooks.useWorksheetStore)();
  const {
    isAutoFillActive,
    onFillFullColumn,
    setIsMouseDown,
    setSelectingActive,
    setBlockFillOut,
    setSelectedCell: setHighlightedCell
  } = (0, _hooks.useAutoFilling)(cell);
  const tooltipId = (0, _react.useId)();
  const setSelectedRows = useStore(store, (0, _shallow.useShallow)(state => state.setSelectedRows));
  const setSelectedCells = useStore(store, (0, _shallow.useShallow)(state => state.setSelectedCells));
  const addInvalidCells = useStore(store, (0, _shallow.useShallow)(state => state.addInvalidCells));
  const removeInvalidCells = useStore(store, (0, _shallow.useShallow)(state => state.removeInvalidCells));
  const row = useStore(store, (0, _shallow.useShallow)(state => state.rows[rowIndex]));
  const editWithValue = useStore(store, (0, _shallow.useShallow)(state => state.editWithValue));
  const isShiftPressed = useStore(store, (0, _shallow.useShallow)(state => state.isShiftPressed));
  const isMetaKey = useStore(store, (0, _shallow.useShallow)(state => state.isMetaKey));
  const isControlKey = useStore(store, (0, _shallow.useShallow)(state => state.isControlKey));
  const {
    selectedCells,
    isLastSelected,
    isFirstSelected,
    isSelected
  } = useStore(store, (0, _shallow.useShallow)(state => {
    const idx = Object.keys(state.selectedCellsMap).indexOf(cellIdx);
    return {
      selectedCells: state.selectedCells,
      isLastSelected: state.selectedCells.length - 1 === idx,
      isFirstSelected: idx === 0,
      isSelected: idx !== -1
    };
  }));
  const isEdited = useStore(store, (0, _shallow.useShallow)(state => !!state.editedCellsMap[cellIdx]));
  const invalidCell = useStore(store, (0, _shallow.useShallow)(state => state.invalidCellsMap[cellIdx]));
  const isValid = (0, _react.useMemo)(() => typeof validation === 'function' ? validation(value) : true, [validation, value]);
  const isNextCellValid = (0, _react.useMemo)(() => typeof validation === 'function' ? validation(nextRowValue) : true, [nextRowValue, validation]);
  (0, _react.useEffect)(() => {
    // Remove from invalidCells if new value is valid
    if (isValid && invalidCell) {
      removeInvalidCells([cell]);
    }

    // Add to invalidCells but only if value is different
    if (!isValid && (!invalidCell || invalidCell.value !== cell.value)) {
      addInvalidCells([cell]);
    }
  }, [addInvalidCells, cell, isValid, invalidCell, removeInvalidCells]);
  const handleAutoFilldblClick = (0, _react.useCallback)(event => {
    event.stopPropagation();
    onFillFullColumn();
  }, [onFillFullColumn]);
  const handleClick = (0, _react.useCallback)(() => {
    if (isShiftPressed) {
      const lastSelected = selectedCells[selectedCells.length - 1];
      const fromIdx = lastSelected.rowIndex;
      const toIdx = cell.rowIndex;
      const rangeIdxs = Array.from({
        length: toIdx - fromIdx
      }, (_, index) => fromIdx + index + 1);
      const newCells = rangeIdxs.map(rowIndex => ({
        ...cell,
        columnIndex: lastSelected.columnIndex,
        rowIndex
      }));
      setSelectedCells([...selectedCells, ...newCells]);
    } else {
      setSelectedRows([rowIndex]);
      setSelectedCells([cell]);
    }
  }, [cell, isShiftPressed, rowIndex, selectedCells, setSelectedCells, setSelectedRows]);
  const renderedValue = (0, _react.useMemo)(() => {
    if (typeof formatting === 'function' && value !== '' && !Number.isNaN(Number(value))) {
      return formatting(value);
    }
    return `${value}`;
  }, [formatting, value]);
  const renderedCell = (0, _react.useMemo)(() => {
    switch (type) {
      case 'select':
        return /*#__PURE__*/(0, _jsxRuntime.jsx)(_editors.SelectEditor, {
          cell: cell,
          isEditing: isEditing,
          onBlur: handleBlur,
          onChange: handleChange,
          options: options
        });
      case 'multiSelect':
        return /*#__PURE__*/(0, _jsxRuntime.jsx)(_MultiSelectEditor.MultiSelectEditor, {
          cell: cell,
          isEditing: isEditing,
          onBlur: handleBlur,
          onChange: handleChange,
          options: options
        });
      case 'checkbox':
        return /*#__PURE__*/(0, _jsxRuntime.jsx)(_editors.CheckboxEditor, {
          cell: cell,
          onBlur: handleBlur,
          onChange: handleChange,
          toggle: isEditing
        });
      case 'modal':
        return /*#__PURE__*/(0, _jsxRuntime.jsx)(_editors.ModalEditor, {
          cell: cell,
          formatting: formatting,
          isEditing: isEditing
        });
      case 'toggle':
        return /*#__PURE__*/(0, _jsxRuntime.jsx)(_editors.ToggleEditor, {
          rowId: rowId,
          toggle: isEditing
        });
      default:
        return isEditing && !disabled ? /*#__PURE__*/(0, _jsxRuntime.jsx)(_editors.TextEditor, {
          action: action,
          cell: cell,
          initialValue: editWithValue,
          isControlKey: isControlKey,
          isEdited: isEdited,
          isMetaKey: isMetaKey,
          onBlur: handleBlur,
          onKeyDown: handleKeyDown
        }) : /*#__PURE__*/(0, _jsxRuntime.jsx)(_Typography.Small, {
          color: disabled ? 'secondary50' : 'secondary70',
          ellipsis: true,
          title: renderedValue,
          children: renderedValue
        });
    }
  }, [type, cell, isEditing, handleBlur, handleChange, options, formatting, rowId, disabled, action, editWithValue, isControlKey, isEdited, isMetaKey, handleKeyDown, renderedValue]);
  const renderedNote = (0, _react.useMemo)(() => {
    if (!notation) {
      return null;
    }
    const note = notation(value, row);
    if (!note) {
      return null;
    }
    return /*#__PURE__*/(0, _jsxRuntime.jsx)(_Tooltip.Tooltip, {
      id: tooltipId,
      placement: "right",
      trigger: /*#__PURE__*/(0, _jsxRuntime.jsx)(_styled.CellNote, {
        color: note.color,
        role: "note"
      }),
      children: note.description
    });
  }, [notation, row, tooltipId, value]);
  const renderedAutoFillHandler = (0, _react.useMemo)(() => {
    return isLastSelected ? /*#__PURE__*/(0, _jsxRuntime.jsx)(_styled.AutoFillHandler, {
      $isVisible: !isAutoFillActive,
      "aria-label": "Autofill handler",
      onDoubleClick: handleAutoFilldblClick,
      onMouseDown: event => {
        event.stopPropagation();
        setSelectingActive(false);
        setBlockFillOut(false);
        setIsMouseDown(true);
      }
    }) : null;
  }, [handleAutoFilldblClick, isAutoFillActive, isLastSelected, setBlockFillOut, setIsMouseDown, setSelectingActive]);
  return /*#__PURE__*/(0, _jsxRuntime.jsxs)(_styled.StyledCell, {
    $isChild: isChild,
    $isEdited: isEdited,
    $isFirstSelected: isFirstSelected,
    $isLastChild: isLastChild,
    $isLastSelected: isLastSelected,
    $isNextCellValid: isNextCellValid,
    $isSelected: isSelected,
    $isValid: isValid,
    $type: type,
    onClick: handleClick,
    onDoubleClick: handleDoubleClick,
    onMouseDown: () => {
      handleClick();
      setSelectingActive(true);
      setBlockFillOut(true);
    },
    onMouseEnter: setHighlightedCell,
    onMouseUp: () => {
      setIsMouseDown(false);
      setSelectingActive(false);
    },
    children: [renderedCell, renderedAutoFillHandler, renderedNote]
  });
};
const Cell = exports.Cell = (0, _utils.typedMemo)(InternalCell);
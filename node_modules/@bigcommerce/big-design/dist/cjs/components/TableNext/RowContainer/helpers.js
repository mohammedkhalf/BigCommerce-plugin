"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.calculateColSpan = void 0;
// prettier-ignore
const calculateColSpan = ({
  columns,
  isExpandable,
  isDraggable,
  isSelectable
}) => {
  let totalColSpans = columns.length;
  if (isExpandable) {
    totalColSpans += 1;
  }
  if (isDraggable) {
    totalColSpans += 1;
  }
  if (isSelectable) {
    totalColSpans += 1;
  }
  return totalColSpans;
};
exports.calculateColSpan = calculateColSpan;
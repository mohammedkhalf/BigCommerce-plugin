"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard").default;
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Collapse = void 0;
var _react = _interopRequireWildcard(require("react"));
var _CollapsePanel = require("./CollapsePanel");
var _CollapseTrigger = require("./CollapseTrigger");
var _useCollapseContext = require("./useCollapseContext");
var _jsxRuntime = require("react/jsx-runtime");
const Collapse = ({
  children,
  isOpen,
  initiallyOpen = false,
  onCollapseChange,
  disabled
}) => {
  const wasDisabled = (0, _react.useRef)(disabled);
  const isControlled = isOpen !== undefined;
  const [uncontrolledOpen, setUncontrolledOpen] = (0, _react.useState)(disabled ? false : initiallyOpen);
  const isCollapseOpen = isControlled ? isOpen : uncontrolledOpen;
  const triggerId = (0, _react.useId)();
  const panelId = (0, _react.useId)();
  (0, _react.useEffect)(() => {
    const justDisabled = disabled && !wasDisabled.current;
    wasDisabled.current = disabled;
    if (justDisabled && isCollapseOpen) {
      if (!isControlled) {
        setUncontrolledOpen(false);
      }
      onCollapseChange?.(false);
    }
  }, [disabled, isControlled, isCollapseOpen, onCollapseChange]);
  const toggle = (0, _react.useCallback)(() => {
    if (disabled) {
      return;
    }
    const next = !isCollapseOpen;
    if (!isControlled) {
      setUncontrolledOpen(next);
    }
    onCollapseChange?.(next);
  }, [disabled, isControlled, isCollapseOpen, onCollapseChange]);
  return /*#__PURE__*/(0, _jsxRuntime.jsx)(_useCollapseContext.CollapseContext.Provider, {
    value: {
      isOpen: isCollapseOpen,
      toggle,
      disabled,
      triggerId,
      panelId
    },
    children: children
  });
};
exports.Collapse = Collapse;
Collapse.Trigger = _CollapseTrigger.CollapseTrigger;
Collapse.Panel = _CollapsePanel.CollapsePanel;
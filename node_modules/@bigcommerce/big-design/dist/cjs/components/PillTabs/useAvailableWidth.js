"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useAvailableWidth = void 0;
var _react = require("react");
var _hooks = require("../../hooks");
const useAvailableWidth = ({
  parent,
  dropdown
}) => {
  const [value, setValue] = (0, _react.useState)(0);
  const update = (0, _react.useCallback)(() => {
    const parentWidth = parent.current?.offsetWidth;
    const dropdownWidth = dropdown.current?.offsetWidth;

    // Check for undefined/null (refs not attached) rather than falsy (0 is valid)
    if (parentWidth == null || dropdownWidth == null) {
      return;
    }
    setValue(parentWidth - dropdownWidth);
  }, [parent, dropdown]);
  (0, _react.useEffect)(update, [update]);
  (0, _hooks.useWindowResizeListener)(update);
  return value;
};
exports.useAvailableWidth = useAvailableWidth;
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.excludePaddingProps = excludePaddingProps;
exports.toTransientPaddingProps = toTransientPaddingProps;
exports.withPaddings = void 0;
var _styledComponents = require("styled-components");
var _spacings = require("../spacings");
// Internal, transient (`$`-prefixed) counterpart of `PaddingProps`. styled-components
// never forwards `$`-prefixed props to the DOM, so tag-target styled components read
// these instead of the public names to avoid leaking them onto real DOM nodes.

const withPaddings = () => (0, _styledComponents.css)(["", ";", ";", ";", ";", ";", ";", ";"], ({
  $padding,
  theme
}) => $padding && (0, _spacings.getSpacingStyles)($padding, theme, 'padding'), ({
  $paddingTop,
  theme
}) => $paddingTop && (0, _spacings.getSpacingStyles)($paddingTop, theme, 'padding-top'), ({
  $paddingRight,
  theme
}) => $paddingRight && (0, _spacings.getSpacingStyles)($paddingRight, theme, 'padding-right'), ({
  $paddingBottom,
  theme
}) => $paddingBottom && (0, _spacings.getSpacingStyles)($paddingBottom, theme, 'padding-bottom'), ({
  $paddingLeft,
  theme
}) => $paddingLeft && (0, _spacings.getSpacingStyles)($paddingLeft, theme, 'padding-left'), ({
  $paddingVertical,
  theme
}) => $paddingVertical && (0, _spacings.getSpacingStyles)($paddingVertical, theme, 'padding-top', 'padding-bottom'), ({
  $paddingHorizontal,
  theme
}) => $paddingHorizontal && (0, _spacings.getSpacingStyles)($paddingHorizontal, theme, 'padding-left', 'padding-right'));
exports.withPaddings = withPaddings;
function excludePaddingProps(props) {
  const {
    padding,
    paddingTop,
    paddingRight,
    paddingBottom,
    paddingLeft,
    paddingVertical,
    paddingHorizontal,
    ...rest
  } = props;
  return rest;
}

// Rename-and-keep counterpart of `excludePaddingProps`: maps the public padding props
// to their transient (`$`-prefixed) names for hand-off to a tag-target styled component.
function toTransientPaddingProps(props) {
  const {
    padding,
    paddingTop,
    paddingRight,
    paddingBottom,
    paddingLeft,
    paddingVertical,
    paddingHorizontal
  } = props;
  return {
    $padding: padding,
    $paddingTop: paddingTop,
    $paddingRight: paddingRight,
    $paddingBottom: paddingBottom,
    $paddingLeft: paddingLeft,
    $paddingVertical: paddingVertical,
    $paddingHorizontal: paddingHorizontal
  };
}
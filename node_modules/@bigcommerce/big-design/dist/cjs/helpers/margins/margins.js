"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.excludeMarginProps = excludeMarginProps;
exports.toTransientMarginProps = toTransientMarginProps;
exports.withMargins = void 0;
var _styledComponents = require("styled-components");
var _spacings = require("../spacings");
// Internal, transient (`$`-prefixed) counterpart of `MarginProps`. styled-components
// never forwards `$`-prefixed props to the DOM, so tag-target styled components read
// these instead of the public names to avoid leaking them onto real DOM nodes.

const withMargins = () => (0, _styledComponents.css)(["", ";", ";", ";", ";", ";", ";", ";"], ({
  $margin,
  theme
}) => $margin && (0, _spacings.getSpacingStyles)($margin, theme, 'margin'), ({
  $marginTop,
  theme
}) => $marginTop && (0, _spacings.getSpacingStyles)($marginTop, theme, 'margin-top'), ({
  $marginRight,
  theme
}) => $marginRight && (0, _spacings.getSpacingStyles)($marginRight, theme, 'margin-right'), ({
  $marginBottom,
  theme
}) => $marginBottom && (0, _spacings.getSpacingStyles)($marginBottom, theme, 'margin-bottom'), ({
  $marginLeft,
  theme
}) => $marginLeft && (0, _spacings.getSpacingStyles)($marginLeft, theme, 'margin-left'), ({
  $marginVertical,
  theme
}) => $marginVertical && (0, _spacings.getSpacingStyles)($marginVertical, theme, 'margin-top', 'margin-bottom'), ({
  $marginHorizontal,
  theme
}) => $marginHorizontal && (0, _spacings.getSpacingStyles)($marginHorizontal, theme, 'margin-left', 'margin-right'));
exports.withMargins = withMargins;
function excludeMarginProps(props) {
  const {
    margin,
    marginTop,
    marginRight,
    marginBottom,
    marginLeft,
    marginVertical,
    marginHorizontal,
    ...rest
  } = props;
  return rest;
}

// Rename-and-keep counterpart of `excludeMarginProps`: maps the public margin props
// to their transient (`$`-prefixed) names for hand-off to a tag-target styled component.
function toTransientMarginProps(props) {
  const {
    margin,
    marginTop,
    marginRight,
    marginBottom,
    marginLeft,
    marginVertical,
    marginHorizontal
  } = props;
  return {
    $margin: margin,
    $marginTop: marginTop,
    $marginRight: marginRight,
    $marginBottom: marginBottom,
    $marginLeft: marginLeft,
    $marginVertical: marginVertical,
    $marginHorizontal: marginHorizontal
  };
}
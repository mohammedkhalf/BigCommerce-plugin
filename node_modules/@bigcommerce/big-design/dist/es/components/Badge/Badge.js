import React, { memo } from 'react';
import { toTransientMarginProps } from '../../helpers/margins/margins';
import { StyledBadge } from './styled';
import { jsx as _jsx } from "react/jsx-runtime";
export const Badge = /*#__PURE__*/memo(props => {
  const {
    className,
    style,
    label,
    variant,
    margin,
    marginTop,
    marginRight,
    marginBottom,
    marginLeft,
    marginVertical,
    marginHorizontal,
    ...domProps
  } = props;
  return typeof label === 'string' ? /*#__PURE__*/_jsx(StyledBadge, {
    ...domProps,
    ...toTransientMarginProps(props),
    $variant: variant ?? 'secondary',
    children: label
  }) : null;
});
Badge.displayName = 'Badge';
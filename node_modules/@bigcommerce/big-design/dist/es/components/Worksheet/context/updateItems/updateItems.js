import React, { createContext, useCallback, useMemo } from 'react';
import { useShallow } from 'zustand/shallow';
import { typedMemo } from '../../../../utils';
import { useWorksheetStore } from '../../hooks';
import { jsx as _jsx } from "react/jsx-runtime";
export const UpdateItemsContext = /*#__PURE__*/createContext(null);
export const UpdateItemsProvider = typedMemo(({
  children,
  items
}) => {
  const {
    store,
    useStore
  } = useWorksheetStore();
  const setRows = useStore(store, useShallow(state => state.setRows));
  const addEditedCells = useStore(store, useShallow(state => state.addEditedCells));
  const updateItems = useCallback((cells, newValues) => {
    setRows(cells.reduce((accum, cell, index) => {
      const {
        hash,
        rowIndex
      } = cell;

      // Don't change since value is the same
      if (cell.value === newValues[index]) {
        return accum;
      }
      const row = accum[rowIndex];
      const updatedRow = {
        ...row,
        [hash]: newValues[index]
      };
      accum[rowIndex] = updatedRow;
      return accum;
    }, items));
    addEditedCells(cells.reduce((accum, cell, index) => {
      // Don't add since value is the same
      if (cell.value === newValues[index]) {
        return accum;
      }
      return [...accum, {
        ...cell,
        value: newValues[index]
      }];
    }, []));
  }, [addEditedCells, items, setRows]);
  const providerValue = useMemo(() => ({
    updateItems
  }), [updateItems]);
  return /*#__PURE__*/_jsx(UpdateItemsContext.Provider, {
    value: providerValue,
    children: children
  });
});
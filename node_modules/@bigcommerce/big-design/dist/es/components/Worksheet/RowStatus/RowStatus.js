import React, { memo } from 'react';
import { useShallow } from 'zustand/shallow';
import { useWorksheetStore } from '../hooks';
import { Status } from './styled';
import { jsx as _jsx } from "react/jsx-runtime";
export const RowStatus = /*#__PURE__*/memo(({
  rowIndex
}) => {
  const {
    store,
    useStore
  } = useWorksheetStore();
  const isSelected = useStore(store, useShallow(state => state.selectedRows.includes(rowIndex)));
  const isInvalid = useStore(store, useShallow(state => state.invalidCells.some(invalidCell => invalidCell.rowIndex === rowIndex)));
  return /*#__PURE__*/_jsx(Status, {
    $isInvalid: isInvalid,
    $isSelected: isSelected
  });
});
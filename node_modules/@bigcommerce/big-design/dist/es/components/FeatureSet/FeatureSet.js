import React, { memo, createElement as _createElement } from 'react';
import { toTransientMarginProps } from '../../helpers/margins/margins';
import { StyledUl } from './styled';
import { Tag } from './Tag';
import { jsx as _jsx } from "react/jsx-runtime";
export const FeatureSet = /*#__PURE__*/memo(props => {
  const {
    tags,
    className,
    style,
    margin,
    marginTop,
    marginRight,
    marginBottom,
    marginLeft,
    marginVertical,
    marginHorizontal,
    ...domProps
  } = props;
  return tags && tags.length > 0 ? /*#__PURE__*/_jsx(StyledUl, {
    ...domProps,
    ...toTransientMarginProps(props),
    children: tags.map((tag, index) => /*#__PURE__*/_createElement(Tag, {
      ...tag,
      key: index
    }))
  }) : null;
});
FeatureSet.displayName = 'FeatureSet';
import React, { useMemo } from 'react';
import { Badge } from '../Badge';
import { Box } from '../Box';
import { Flex } from '../Flex';
import { FormControlDescription } from '../Form';
import { StyleableText } from '../Typography/Typography';
import { InfoCardImgContainer } from './styled';
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
export const InfoCard = ({
  img,
  title,
  badge,
  description
}) => {
  const {
    className: imgClassName,
    style: imgStyle,
    ...imgProps
  } = img ?? {};
  const renderedDescription = useMemo(() => {
    if (!description) {
      return null;
    }
    const link = typeof description === 'object' ? description.link : undefined;
    const text = typeof description === 'object' ? description.text : description;
    return /*#__PURE__*/_jsx(FormControlDescription, {
      link: link,
      children: text
    });
  }, [description]);
  return /*#__PURE__*/_jsxs(Flex, {
    alignItems: "center",
    children: [img && /*#__PURE__*/_jsx(InfoCardImgContainer, {
      height: 40,
      width: 40,
      ...imgProps,
      alt: img.alt ?? ''
    }), /*#__PURE__*/_jsxs(Box, {
      children: [/*#__PURE__*/_jsxs(StyleableText, {
        $margin: "none",
        children: [title, badge ? /*#__PURE__*/_jsx(Badge, {
          marginLeft: "xSmall",
          ...badge
        }) : null]
      }), renderedDescription]
    })]
  });
};
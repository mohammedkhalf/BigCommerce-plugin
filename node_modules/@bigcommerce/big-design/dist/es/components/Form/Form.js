import React, { forwardRef } from 'react';
import { toTransientMarginProps } from '../../helpers/margins/margins';
import { typedMemo } from '../../utils';
import { StyledForm } from './styled';
import { FormContext } from './useFormContext';
import { jsx as _jsx } from "react/jsx-runtime";
const StyleableForm = props => {
  const {
    forwardedRef,
    fullWidth,
    margin,
    marginTop,
    marginRight,
    marginBottom,
    marginLeft,
    marginVertical,
    marginHorizontal,
    ...domProps
  } = props;
  return /*#__PURE__*/_jsx(FormContext.Provider, {
    value: {
      fullWidth
    },
    children: /*#__PURE__*/_jsx(StyledForm, {
      ...domProps,
      ...toTransientMarginProps(props),
      $fullWidth: fullWidth,
      ref: forwardedRef
    })
  });
};
export const Form = typedMemo(/*#__PURE__*/forwardRef(({
  className,
  style,
  ...props
}, ref) => /*#__PURE__*/_jsx(StyleableForm, {
  ...props,
  forwardedRef: ref
})));
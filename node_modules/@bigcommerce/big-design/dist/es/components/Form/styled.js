import { theme as defaultTheme } from '@bigcommerce/big-design-theme';
import styled from 'styled-components';
import { withMargins } from '../../helpers';
import { StyledFileUploaderWrapper } from '../FileUploader/styled';
import { StyledInputWrapper } from '../Input/styled';
import { StyledTextareaWrapper } from '../Textarea/styled';
export const StyledForm = /*#__PURE__*/styled.form.withConfig({
  displayName: "styled__StyledForm",
  componentId: "sc-qzey9b-0"
})(["", " ", "{", ",", ",", "{max-width:", ";}}"], withMargins(), ({
  theme
}) => theme.breakpoints.tablet, StyledInputWrapper, StyledTextareaWrapper, StyledFileUploaderWrapper, ({
  $fullWidth,
  theme
}) => $fullWidth ? '100%' : theme.helpers.remCalc(416));
StyledForm.defaultProps = {
  theme: defaultTheme
};
import { theme as defaultTheme } from '@bigcommerce/big-design-theme';
import { normalize } from 'polished';
import React, { useContext } from 'react';
import { createGlobalStyle, ThemeContext } from 'styled-components';
import { jsx as _jsx } from "react/jsx-runtime";
const BaseGlobalStyles = /*#__PURE__*/createGlobalStyle(["", " body{font-family:", ";}"], normalize(), ({
  $fontFamily
}) => $fontFamily);
export const GlobalStyles = () => {
  // ThemeContext returns undefined when no ThemeProvider is present; fall back to defaultTheme
  const theme = useContext(ThemeContext) ?? defaultTheme;
  return /*#__PURE__*/_jsx(BaseGlobalStyles, {
    $fontFamily: theme.typography.fontFamily
  });
};
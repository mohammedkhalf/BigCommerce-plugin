import { draggable, dropTargetForElements } from '@atlaskit/pragmatic-drag-and-drop/adapter/element-adapter';
import { combine } from '@atlaskit/pragmatic-drag-and-drop/utils/combine';
import { preserveOffsetOnSource } from '@atlaskit/pragmatic-drag-and-drop/utils/preserve-offset-on-source';
import { setCustomNativeDragPreview } from '@atlaskit/pragmatic-drag-and-drop/utils/set-custom-native-drag-preview';
import { attachClosestEdge } from '@atlaskit/pragmatic-drag-and-drop-hitbox/closest-edge';
import { announce } from '@atlaskit/pragmatic-drag-and-drop-live-region';
import { DragIndicatorIcon } from '@bigcommerce/big-design-icons';
import React, { useEffect, useRef, useState } from 'react';
import { createPortal } from 'react-dom';
import { typedMemo } from '../../../utils';
import { Checkbox } from '../../Checkbox';
import { DataCell } from '../DataCell';
import { StyledDragHandle, StyledDragPreview, StyledTableRow } from './styled';
import { jsx as _jsx, jsxs as _jsxs, Fragment as _Fragment } from "react/jsx-runtime";
const InternalRow = ({
  columns,
  index,
  isHidden = false,
  isPhantom = false,
  isSelectable = false,
  isSelected = false,
  item,
  itemCount,
  showDragIcon = false,
  tableId,
  onItemSelect,
  onRowDrop,
  ...rest
}) => {
  const rowRef = useRef(null);
  const dragHandleRef = useRef(null);
  const [isDragging, setIsDragging] = useState(false);
  const [isGrabbed, setIsGrabbed] = useState(false);
  const [dragPreview, setDragPreview] = useState(null);
  useEffect(() => {
    const rowElement = rowRef.current;
    const dragHandleElement = dragHandleRef.current;
    if (isPhantom || !showDragIcon || !rowElement || !dragHandleElement) {
      return;
    }

    /* istanbul ignore next -- pointer drag callbacks only run in a real browser */
    return combine(draggable({
      element: rowElement,
      dragHandle: dragHandleElement,
      getInitialData: () => ({
        index,
        tableId
      }),
      onGenerateDragPreview: ({
        location,
        nativeSetDragImage
      }) => {
        const {
          width
        } = rowElement.getBoundingClientRect();
        setCustomNativeDragPreview({
          nativeSetDragImage,
          getOffset: preserveOffsetOnSource({
            element: rowElement,
            input: location.current.input
          }),
          render: ({
            container
          }) => {
            setDragPreview({
              container,
              width
            });
            return () => setDragPreview(null);
          }
        });
      },
      onDragStart: () => setIsDragging(true),
      onDrop: () => setIsDragging(false)
    }), dropTargetForElements({
      element: rowElement,
      canDrop: ({
        source
      }) => source.data.tableId === tableId,
      getData: ({
        input,
        element
      }) => attachClosestEdge({
        index
      }, {
        element,
        input,
        allowedEdges: ['top', 'bottom']
      }),
      getIsSticky: () => true
    }));
  }, [index, isPhantom, showDragIcon, tableId]);
  const onChange = () => {
    if (onItemSelect) {
      onItemSelect(item);
    }
  };
  const onDragHandleKeyDown = event => {
    /* istanbul ignore next -- defensive: the handle only renders when onRowDrop is set */
    if (typeof onRowDrop !== 'function') {
      return;
    }
    const {
      key
    } = event;
    if (key === ' ' || key === 'Enter') {
      event.preventDefault();
      const nextGrabbed = !isGrabbed;
      setIsGrabbed(nextGrabbed);
      announce(nextGrabbed ? `Grabbed row ${index + 1} of ${itemCount}. Use the arrow keys to move, space to drop.` : `Dropped row at position ${index + 1} of ${itemCount}.`);
      return;
    }
    if (!isGrabbed) {
      return;
    }
    if (key === 'ArrowUp' && index > 0) {
      event.preventDefault();
      onRowDrop(index, index - 1);
      announce(`Moved row to position ${index} of ${itemCount}.`);
    } else if (key === 'ArrowDown' && index < itemCount - 1) {
      event.preventDefault();
      onRowDrop(index, index + 1);
      announce(`Moved row to position ${index + 2} of ${itemCount}.`);
    } else if (key === 'Escape') {
      event.preventDefault();
      setIsGrabbed(false);
      announce('Movement cancelled.');
    }
  };
  const label = isSelected ? `Selected` : `Unselected`;
  const dataCells = columns.map(({
    render: CellContent,
    align,
    display,
    verticalAlign,
    width,
    withPadding = true
  }, columnIndex) => /*#__PURE__*/_jsx(DataCell, {
    align: align,
    display: display,
    verticalAlign: verticalAlign,
    width: width,
    withPadding: withPadding,
    children: /*#__PURE__*/_jsx(CellContent, {
      ...item
    })
  }, columnIndex));

  /* istanbul ignore next -- the custom drag preview only renders during a real browser drag */
  const dragPreviewPortal = dragPreview !== null ? /*#__PURE__*/createPortal(/*#__PURE__*/_jsx(StyledDragPreview, {
    style: {
      width: dragPreview.width
    },
    children: /*#__PURE__*/_jsx("tbody", {
      children: /*#__PURE__*/_jsxs("tr", {
        children: [showDragIcon && /*#__PURE__*/_jsx(DataCell, {
          children: /*#__PURE__*/_jsx(DragIndicatorIcon, {})
        }), dataCells]
      })
    })
  }), dragPreview.container) : null;
  return /*#__PURE__*/_jsxs(_Fragment, {
    children: [/*#__PURE__*/_jsxs(StyledTableRow, {
      $isDragging: isDragging,
      $isGrabbed: isGrabbed,
      $isHidden: isHidden,
      $isPhantom: isPhantom,
      $isSelected: isSelected,
      ref: rowRef,
      ...rest,
      children: [showDragIcon && /*#__PURE__*/_jsx(DataCell, {
        children: isPhantom ? /*#__PURE__*/_jsx(DragIndicatorIcon, {}) : /*#__PURE__*/_jsx(StyledDragHandle, {
          "aria-label": `Reorder row ${index + 1} of ${itemCount}`,
          "aria-pressed": isGrabbed,
          onBlur: () => setIsGrabbed(false),
          onKeyDown: onDragHandleKeyDown,
          ref: dragHandleRef,
          type: "button",
          children: /*#__PURE__*/_jsx(DragIndicatorIcon, {})
        })
      }), isSelectable && /*#__PURE__*/_jsx(DataCell, {
        isCheckbox: true,
        children: /*#__PURE__*/_jsx(Checkbox, {
          checked: isSelected,
          hiddenLabel: true,
          label: label,
          onChange: onChange
        })
      }, "data-checkbox"), dataCells]
    }), dragPreviewPortal]
  });
};
export const Row = typedMemo(InternalRow);
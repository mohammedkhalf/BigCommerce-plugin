export const discriminatePagination = pagination => {
  if ('onPageChange' in pagination) {
    return {
      type: 'offset',
      ...pagination
    };
  }
  return {
    type: 'stateless',
    ...pagination
  };
};
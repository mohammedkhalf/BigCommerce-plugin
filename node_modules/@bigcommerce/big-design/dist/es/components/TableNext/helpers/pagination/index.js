export function getPagedIndex(index, pagination) {
  const {
    currentPage,
    itemsPerPage
  } = {
    currentPage: 1,
    itemsPerPage: 0,
    ...pagination
  };
  return index + (currentPage - 1) * itemsPerPage;
}
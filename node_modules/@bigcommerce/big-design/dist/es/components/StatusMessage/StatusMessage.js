import { theme } from '@bigcommerce/big-design-theme';
import React from 'react';
import { Flex } from '../Flex';
import { H1, H4, Text } from '../Typography';
import { StyledStatusIllustration, StyledStatusMessage } from './styled';
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
export const StatusMessage = ({
  actions,
  heading,
  message,
  size = 'panel',
  variant = 'info'
}) => {
  const Header = size === 'page' ? H1 : H4;
  const hasHeading = heading !== undefined && heading.length > 0;
  const flexGapSize = size === 'page' ? theme.spacing.xLarge : theme.spacing.medium;
  const paddingVertical = size === 'page' ? 'xxxLarge' : 'large';
  return /*#__PURE__*/_jsxs(Flex, {
    alignItems: "center",
    flexDirection: "column",
    flexGap: flexGapSize,
    justifyContent: "center",
    paddingVertical: paddingVertical,
    children: [/*#__PURE__*/_jsx(StyledStatusIllustration, {
      $size: size,
      $variant: variant,
      "aria-hidden": "true"
    }), /*#__PURE__*/_jsxs(StyledStatusMessage, {
      children: [hasHeading && /*#__PURE__*/_jsx(Header, {
        margin: "none",
        marginBottom: "small",
        children: heading
      }), /*#__PURE__*/_jsx(Text, {
        margin: "none",
        children: message
      })]
    }), actions]
  });
};
StatusMessage.displayName = 'StatusMessage';
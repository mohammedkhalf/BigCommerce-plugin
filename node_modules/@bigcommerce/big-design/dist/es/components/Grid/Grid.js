import React, { forwardRef } from 'react';
import { StyledGrid } from './styled';
import { toTransientGridedContainerProps } from './withGrid';
import { jsx as _jsx } from "react/jsx-runtime";
// `display` is passed twice deliberately: StyledGrid composes over Box (a component, not
// a tag), so `display` also needs to reach Box's own internal $-prefixed handling
// untouched (its public contract), while `$display` feeds StyledGrid's own separate
// withDisplay() call on its outer layer.
const RawGrid = props => {
  const {
    as,
    display,
    forwardedRef,
    gridAreas,
    gridAutoColumns,
    gridAutoFlow,
    gridAutoRows,
    gridColumns,
    gridColumnGap,
    gridGap,
    gridRows,
    gridRowGap,
    gridTemplate,
    ...domProps
  } = props;
  return /*#__PURE__*/_jsx(StyledGrid, {
    $display: display,
    display: display,
    forwardedAs: as,
    ref: forwardedRef,
    ...domProps,
    ...toTransientGridedContainerProps(props)
  });
};
export const Grid = /*#__PURE__*/forwardRef((props, ref) => /*#__PURE__*/_jsx(RawGrid, {
  ...props,
  forwardedRef: ref
}));
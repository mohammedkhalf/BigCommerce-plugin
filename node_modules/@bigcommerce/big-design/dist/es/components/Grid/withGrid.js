import { breakpointsOrder } from '@bigcommerce/big-design-theme';
import { css } from 'styled-components';
export const withGridedContainer = () => css(["", ";", ";", ";", ";", ";", ";", ";", ";", ";", ";"], ({
  $gridAreas,
  theme
}) => $gridAreas && getGridedStyles($gridAreas, theme, 'grid-template-areas'), ({
  $gridAutoColumns,
  theme
}) => $gridAutoColumns && getGridedStyles($gridAutoColumns, theme, 'grid-auto-columns'), ({
  $gridAutoFlow,
  theme
}) => $gridAutoFlow && getGridedStyles($gridAutoFlow, theme, 'grid-auto-flow'), ({
  $gridAutoRows,
  theme
}) => $gridAutoRows && getGridedStyles($gridAutoRows, theme, 'grid-auto-rows'), ({
  $gridColumns,
  theme
}) => $gridColumns && getGridedStyles($gridColumns, theme, 'grid-template-columns'), ({
  $gridGap,
  theme
}) => getGridedStyles($gridGap ?? theme.spacing.medium, theme, 'gap'), ({
  $gridColumnGap,
  theme
}) => $gridColumnGap && getGridedStyles($gridColumnGap, theme, 'column-gap'), ({
  $gridRows,
  theme
}) => $gridRows && getGridedStyles($gridRows, theme, 'grid-template-rows'), ({
  $gridRowGap,
  theme
}) => $gridRowGap && getGridedStyles($gridRowGap, theme, 'row-gap'), ({
  $gridTemplate,
  theme
}) => $gridTemplate && getGridedStyles($gridTemplate, theme, 'grid-template'));
export const withGridedItems = () => css(["", ";", ";", ";", ";", ";", ";", ";"], ({
  $gridArea,
  theme
}) => $gridArea && getGridedStyles($gridArea, theme, 'grid-area'), ({
  $gridColumn,
  theme
}) => $gridColumn && getGridedStyles($gridColumn, theme, 'grid-column'), ({
  $gridColumnEnd,
  theme
}) => $gridColumnEnd && getGridedStyles($gridColumnEnd, theme, 'grid-column-end'), ({
  $gridColumnStart,
  theme
}) => $gridColumnStart && getGridedStyles($gridColumnStart, theme, 'grid-column-start'), ({
  $gridRow,
  theme
}) => $gridRow && getGridedStyles($gridRow, theme, 'grid-row'), ({
  $gridRowEnd,
  theme
}) => $gridRowEnd && getGridedStyles($gridRowEnd, theme, 'grid-row-end'), ({
  $gridRowStart,
  theme
}) => $gridRowStart && getGridedStyles($gridRowStart, theme, 'grid-row-start'));
export function toTransientGridedContainerProps(props) {
  const {
    gridAreas,
    gridAutoColumns,
    gridAutoFlow,
    gridAutoRows,
    gridColumns,
    gridColumnGap,
    gridGap,
    gridRows,
    gridRowGap,
    gridTemplate
  } = props;
  return {
    $gridAreas: gridAreas,
    $gridAutoColumns: gridAutoColumns,
    $gridAutoFlow: gridAutoFlow,
    $gridAutoRows: gridAutoRows,
    $gridColumns: gridColumns,
    $gridColumnGap: gridColumnGap,
    $gridGap: gridGap,
    $gridRows: gridRows,
    $gridRowGap: gridRowGap,
    $gridTemplate: gridTemplate
  };
}
export function toTransientGridedItemProps(props) {
  const {
    gridArea,
    gridColumn,
    gridColumnEnd,
    gridColumnStart,
    gridRow,
    gridRowEnd,
    gridRowStart
  } = props;
  return {
    $gridArea: gridArea,
    $gridColumn: gridColumn,
    $gridColumnEnd: gridColumnEnd,
    $gridColumnStart: gridColumnStart,
    $gridRow: gridRow,
    $gridRowEnd: gridRowEnd,
    $gridRowStart: gridRowStart
  };
}
const getGridedStyles = (gridedProp, theme, cssKey) => {
  if (typeof gridedProp === 'object') {
    return getResponsiveGrid(gridedProp, theme, cssKey);
  }
  if (typeof gridedProp === 'string' || typeof gridedProp === 'number') {
    return getSimpleGrid(gridedProp, cssKey);
  }
  return [];
};
const getSimpleGrid = (gridedProp, cssKey) => css(["", ":", ""], cssKey, gridedProp);
const getResponsiveGrid = (gridedProp, theme, cssKey) => {
  const breakpointKeys = Object.keys(gridedProp).sort((firstBreakpoint, secondBreakpoint) =>
  // eslint-disable-next-line @typescript-eslint/consistent-type-assertions
  breakpointsOrder.indexOf(firstBreakpoint) -
  // eslint-disable-next-line @typescript-eslint/consistent-type-assertions
  breakpointsOrder.indexOf(secondBreakpoint));

  // eslint-disable-next-line @typescript-eslint/consistent-type-assertions
  return breakpointKeys.map(breakpointKey => css(["", "{", " ", "}"], theme.breakpoints[breakpointKey], '', getSimpleGrid(gridedProp[breakpointKey], cssKey)));
};
import React, { forwardRef, memo, useMemo } from 'react';
import { createLocalizationProvider, getTimeIntervals } from '../../utils';
import { Select } from '../Select';
import { jsx as _jsx } from "react/jsx-runtime";
const RawTimePicker = ({
  error,
  forwardedRef,
  label,
  locale = 'en-US',
  onTimeChange,
  value,
  ...props
}) => {
  const localizationProvider = createLocalizationProvider(locale);
  const options = useMemo(() => getTimeIntervals(localizationProvider), [localizationProvider]);
  return /*#__PURE__*/_jsx(Select, {
    autoWidth: true,
    error: error,
    inputRef: forwardedRef,
    label: label,
    onOptionChange: onTimeChange,
    options: options,
    placeholder: "hh : mm",
    value: value,
    ...props
  });
};
export const Timepicker = /*#__PURE__*/memo(/*#__PURE__*/forwardRef(({
  className,
  style,
  ...props
}, ref) => /*#__PURE__*/_jsx(RawTimePicker, {
  ...props,
  forwardedRef: ref
})));
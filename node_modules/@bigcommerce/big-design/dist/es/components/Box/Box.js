import React, { forwardRef, memo } from 'react';
import { toTransientDisplayProps } from '../../helpers/display/display';
import { toTransientMarginProps } from '../../helpers/margins/margins';
import { toTransientPaddingProps } from '../../helpers/paddings/paddings';
import { StyledBox } from './styled';
import { jsx as _jsx } from "react/jsx-runtime";
const RawBox = props => {
  const {
    forwardedRef,
    display,
    margin,
    marginTop,
    marginRight,
    marginBottom,
    marginLeft,
    marginVertical,
    marginHorizontal,
    padding,
    paddingTop,
    paddingRight,
    paddingBottom,
    paddingLeft,
    paddingVertical,
    paddingHorizontal,
    backgroundColor,
    shadow,
    border,
    borderTop,
    borderRight,
    borderBottom,
    borderLeft,
    borderRadius,
    clearfix,
    zIndex,
    ...domProps
  } = props;
  return /*#__PURE__*/_jsx(StyledBox, {
    ref: forwardedRef,
    ...domProps,
    ...toTransientDisplayProps(props),
    ...toTransientMarginProps(props),
    ...toTransientPaddingProps(props),
    $backgroundColor: backgroundColor,
    $border: border,
    $borderBottom: borderBottom,
    $borderLeft: borderLeft,
    $borderRadius: borderRadius,
    $borderRight: borderRight,
    $borderTop: borderTop,
    $clearfix: clearfix,
    $shadow: shadow,
    $zIndex: zIndex
  });
};
export const Box = /*#__PURE__*/memo(/*#__PURE__*/forwardRef((props, ref) => /*#__PURE__*/_jsx(RawBox, {
  ...props,
  forwardedRef: ref
})));
Box.displayName = 'Box';
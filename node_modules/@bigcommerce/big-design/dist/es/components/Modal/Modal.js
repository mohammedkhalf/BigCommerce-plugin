import { CloseIcon } from '@bigcommerce/big-design-icons';
import { createFocusTrap } from 'focus-trap';
import React, { useEffect, useId, useMemo, useRef, useState } from 'react';
import { createPortal } from 'react-dom';
import { typedMemo } from '../../utils';
import { Button } from '../Button';
import { MessagingButton } from '../Button/private';
import { H2 } from '../Typography';
import { StyledModal, StyledModalActions, StyledModalBody, StyledModalClose, StyledModalContent, StyledModalHeader } from './styled';
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
export const Modal = typedMemo(props => {
  const [modalContainer, setModalContainer] = useState(null);
  useEffect(() => {
    const container = document.createElement('div');
    document.body.appendChild(container);
    setModalContainer(container);
  }, []);
  useEffect(() => {
    return () => {
      if (modalContainer) {
        document.body.removeChild(modalContainer);
      }
    };
  }, [modalContainer]);
  return props.isOpen && modalContainer ? /*#__PURE__*/createPortal(/*#__PURE__*/_jsx(InternalModal, {
    ...props
  }), modalContainer) : null;
});
const InternalModal = ({
  actions,
  backdrop = true,
  children,
  closeOnClickOutside = false,
  closeOnEscKey = true,
  header,
  onClose = () => null,
  variant = 'modal'
}) => {
  const initialBodyOverflowYRef = useRef('');
  const internalTrap = useRef(null);
  const headerUniqueId = useId();
  const [modalRef, setModalRef] = useState(null);
  const onClickAway = event => {
    if (closeOnClickOutside && modalRef === event.target) {
      onClose();
    }
  };
  const onKeyDown = event => {
    if (closeOnEscKey && event.key === 'Escape') {
      onClose();
    }
  };

  // Disable scroll on body when modal is open
  useEffect(() => {
    initialBodyOverflowYRef.current = document.body.style.overflowY || '';
    document.body.style.overflowY = 'hidden';
    return () => {
      document.body.style.overflowY = initialBodyOverflowYRef.current;
    };
  }, []);

  // Setup focus-trap
  useEffect(() => {
    if (modalRef && internalTrap.current === null) {
      internalTrap.current = createFocusTrap(modalRef, {
        allowOutsideClick: event => {
          const target = event.target;

          // Allow clicks on portaled menu/listbox elements (e.g., Dropdown, Select)
          /* istanbul ignore next */
          if (target instanceof Element) {
            return target.closest('[role="menu"]') !== null || target.closest('[role="listbox"]') !== null;
          }
          return false;
        },
        fallbackFocus: modalRef
      });
      internalTrap.current.activate();
    }
    return () => {
      internalTrap.current?.deactivate();
    };
  }, [internalTrap, modalRef]);
  const renderedClose = useMemo(() => variant === 'modal' && /*#__PURE__*/_jsx(StyledModalClose, {
    children: /*#__PURE__*/_jsx(MessagingButton, {
      iconOnly: /*#__PURE__*/_jsx(CloseIcon, {
        title: "Close"
      }),
      onClick: onClose
    })
  }), [onClose, variant]);
  const renderedHeader = useMemo(() => header && typeof header === 'string' && /*#__PURE__*/_jsx(StyledModalHeader, {
    id: headerUniqueId,
    children: /*#__PURE__*/_jsx(H2, {
      margin: "none",
      children: header
    })
  }), [header, headerUniqueId]);
  const renderedActions = useMemo(() => Array.isArray(actions) && /*#__PURE__*/_jsx(StyledModalActions, {
    justifyContent: "flex-end",
    children: actions.map(({
      text,
      onClick,
      ...props
    }, index) => /*#__PURE__*/_jsx(Button, {
      ...props,
      onClick: event => {
        internalTrap.current?.deactivate();
        if (typeof onClick === 'function') {
          onClick(event);
        }
      },
      children: text
    }, index))
  }), [actions]);
  return /*#__PURE__*/_jsx(StyledModal, {
    $backdrop: backdrop,
    $variant: variant,
    onClick: onClickAway,
    onKeyDown: onKeyDown,
    ref: setModalRef,
    tabIndex: -1,
    children: /*#__PURE__*/_jsxs(StyledModalContent, {
      $variant: variant,
      "aria-labelledby": headerUniqueId,
      flexDirection: "column",
      children: [renderedClose, renderedHeader, /*#__PURE__*/_jsx(StyledModalBody, {
        children: children
      }), renderedActions]
    })
  });
};
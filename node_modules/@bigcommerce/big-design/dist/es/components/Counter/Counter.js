import { AddCircleOutlineIcon, RemoveCircleOutlineIcon } from '@bigcommerce/big-design-icons';
import React, { cloneElement, forwardRef, isValidElement, useEffect, useId, useMemo, useState } from 'react';
import { typedMemo, warning } from '../../utils';
import { FormControlDescription, FormControlLabel } from '../Form';
import { useInputErrors } from '../Form/useInputErrors';
import { StyledCounterButton, StyledCounterInput, StyledCounterWrapper } from './styled';
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
const defaultLocalization = {
  decreaseCount: 'Decrease count',
  increaseCount: 'Increase count'
};
export const StylableCounter = typedMemo(({
  min = 0,
  max = 100,
  step = 1,
  forwardedRef,
  label,
  labelId,
  localization = defaultLocalization,
  description,
  error,
  disabled,
  value,
  onCountChange,
  ...props
}) => {
  const [focus, setFocus] = useState(false);
  const uniqueCounterId = useId();
  const id = props.id ? props.id : uniqueCounterId;
  const {
    errors
  } = useInputErrors(id, error);
  useEffect(() => {
    if (!Number.isInteger(value)) {
      onCountChange(Math.round(value));
    }
  }, [onCountChange, value]);
  const handleFocus = event => {
    const {
      onFocus
    } = props;
    setFocus(true);
    if (onFocus) {
      onFocus(event);
    }
  };
  const handleBlur = event => {
    const {
      onBlur
    } = props;
    setFocus(false);
    if (onBlur) {
      onBlur(event);
    }
  };
  const handleIncrease = () => {
    if (value + step > Number(max)) {
      return;
    }

    // Checks that the provided value is a multiple of the step
    if (value % step === 0) {
      onCountChange(value + step);
    }
    // If not, returns nearest higher value that is a multiple of the step
    else {
      onCountChange(value + (step - value % step));
    }
  };
  const handleDecrease = () => {
    if (value - step < Number(min)) {
      return;
    }

    // Checks that the provided value is a multiple of the step
    if (value % step === 0) {
      onCountChange(value - step);
    }
    // If not, returns nearest lower value that is a multiple of the step
    else {
      onCountChange(value - value % step);
    }
  };
  const handleChange = event => {
    const newValue = Number(event.currentTarget.value);
    if (isNaN(newValue)) {
      return;
    }
    if (!Number.isInteger(newValue)) {
      onCountChange(Math.round(newValue));
    }
    if (newValue >= Number(min) && newValue <= Number(max)) {
      onCountChange(newValue);
    }
  };
  const handleKeyPress = event => {
    switch (event.key) {
      case 'ArrowUp':
        handleIncrease();
        break;
      case 'ArrowDown':
        handleDecrease();
        break;
      case 'Escape':
        onCountChange(0);
        break;
      default:
        break;
    }
  };
  const renderedLabel = useMemo(() => {
    if (!label) {
      return null;
    }
    if (typeof label === 'string') {
      return /*#__PURE__*/_jsx(FormControlLabel, {
        htmlFor: id,
        id: labelId,
        required: props.required,
        children: label
      });
    }
    if (/*#__PURE__*/isValidElement(label) && label.type === FormControlLabel) {
      return /*#__PURE__*/cloneElement(label, {
        id: labelId,
        htmlFor: id
      });
    }
    warning('label must be either a string or a FormControlLabel component.');
  }, [id, label, labelId, props.required]);
  const renderedDescription = useMemo(() => {
    if (!description) {
      return null;
    }
    if (typeof description === 'string') {
      return /*#__PURE__*/_jsx(FormControlDescription, {
        children: description
      });
    }
    if (/*#__PURE__*/isValidElement(description) && description.type === FormControlDescription) {
      return description;
    }
    warning('description must be either a string or a FormControlDescription component.');
  }, [description]);
  return /*#__PURE__*/_jsxs("div", {
    children: [renderedLabel, renderedDescription, /*#__PURE__*/_jsxs(StyledCounterWrapper, {
      $error: errors,
      $focus: focus,
      disabled: disabled,
      children: [/*#__PURE__*/_jsx(StyledCounterButton, {
        disabled: disabled || value <= Number(min),
        iconOnly: /*#__PURE__*/_jsx(RemoveCircleOutlineIcon, {
          title: localization.decreaseCount
        }),
        onClick: handleDecrease,
        type: "button"
      }), /*#__PURE__*/_jsx(StyledCounterInput, {
        ...props,
        disabled: disabled,
        id: id,
        onBlur: handleBlur,
        onChange: handleChange,
        onFocus: handleFocus,
        onKeyDown: handleKeyPress,
        ref: forwardedRef,
        value: value
      }), /*#__PURE__*/_jsx(StyledCounterButton, {
        disabled: disabled || value >= Number(max),
        iconOnly: /*#__PURE__*/_jsx(AddCircleOutlineIcon, {
          title: localization.increaseCount
        }),
        onClick: handleIncrease,
        type: "button"
      })]
    })]
  });
});
export const Counter = typedMemo(/*#__PURE__*/forwardRef(({
  className,
  style,
  ...props
}, ref) => /*#__PURE__*/_jsx(StylableCounter, {
  ...props,
  forwardedRef: ref
})));
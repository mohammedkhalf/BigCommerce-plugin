import { theme as defaultTheme } from '@bigcommerce/big-design-theme';
import styled from 'styled-components';
import { withDisplay } from '../../helpers';
import { Box } from '../Box';
import { withFlexedContainer } from './withFlex';
// TODO: Remove the `forwardedAs` manual prop definition when @types get updated
export const StyledFlex = /*#__PURE__*/styled(Box).withConfig({
  displayName: "styled__StyledFlex",
  componentId: "sc-hcvk8l-0"
})(["", " display:flex;", ""], withFlexedContainer(), withDisplay());
StyledFlex.defaultProps = {
  theme: defaultTheme
};
import { theme as defaultTheme } from '@bigcommerce/big-design-theme';
import { ellipsis } from 'polished';
import styled, { css } from 'styled-components';
import { withMargins } from '../../helpers';
import { withTransition } from '../../helpers/transitions';
export const StyledLink = /*#__PURE__*/styled.a.withConfig({
  displayName: "styled__StyledLink",
  componentId: "sc-n9mww9-0"
})(["", ";", " ", ";color:", ";cursor:pointer;font-size:", ";font-weight:", ";text-decoration:none;&:active{color:", ";}&:hover:not(:active){color:", ";}", ""], withMargins(), withTransition(['color'], '70ms'), props => props.$ellipsis && ellipsis(), ({
  theme
}) => theme.colors.primary, ({
  theme
}) => theme.typography.fontSize.medium, ({
  theme
}) => theme.typography.fontWeight.regular, ({
  theme
}) => theme.colors.primary70, ({
  theme
}) => theme.colors.primary70, ({
  $isExternal,
  theme
}) => $isExternal && css(["display:inline-flex;align-items:center;svg{flex-shrink:0;margin-left:", ";}"], theme.spacing.xxSmall));
StyledLink.defaultProps = {
  theme: defaultTheme
};
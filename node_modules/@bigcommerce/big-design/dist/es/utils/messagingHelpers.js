import { CheckCircleIcon, ErrorIcon, InfoIcon, WarningIcon } from '@bigcommerce/big-design-icons';
import React from 'react';
import { css } from 'styled-components';
import { jsx as _jsx } from "react/jsx-runtime";
export const getMessagingIcon = (type, condensed) => {
  const size = condensed ? 'large' : 'xLarge';
  switch (type) {
    case 'success':
      return /*#__PURE__*/_jsx(CheckCircleIcon, {
        color: "success",
        size: size
      });
    case 'error':
      return /*#__PURE__*/_jsx(ErrorIcon, {
        color: "danger",
        size: size
      });
    case 'warning':
      return /*#__PURE__*/_jsx(WarningIcon, {
        color: "warning50",
        size: size
      });
    case 'info':
      return /*#__PURE__*/_jsx(InfoIcon, {
        color: "primary60",
        size: size
      });
  }
};
export const getBorderStyle = (type, theme) => css(["", ";", ";", ";", ";"], type === 'success' && css(["border-left:", " solid ", ";"], theme.spacing.xxSmall, theme.colors.success), type === 'error' && css(["border-left:", " solid ", ";"], theme.spacing.xxSmall, theme.colors.danger), type === 'warning' && css(["border-left:", " solid ", ";"], theme.spacing.xxSmall, theme.colors.warning50), type === 'info' && css(["border-left:", " solid ", ";"], theme.spacing.xxSmall, theme.colors.primary60));
export const getActionVariant = variant => variant === 'primary' ? 'secondary' : variant;
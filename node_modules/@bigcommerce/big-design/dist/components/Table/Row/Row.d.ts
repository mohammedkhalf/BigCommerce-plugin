import React, { type ComponentPropsWithoutRef } from 'react';
import type { TableColumn, TableItem } from '../types';
export interface RowProps<T> extends ComponentPropsWithoutRef<'tr'> {
    columns: Array<TableColumn<T>>;
    index: number;
    item: T;
    itemCount: number;
    isHidden?: boolean;
    isPhantom?: boolean;
    isSelected?: boolean;
    isSelectable?: boolean;
    showDragIcon?: boolean;
    tableId: string;
    onItemSelect?(item: T): void;
    onRowDrop?(from: number, to: number): void;
}
export declare const Row: <T extends TableItem>({ columns, index, isHidden, isPhantom, isSelectable, isSelected, item, itemCount, showDragIcon, tableId, onItemSelect, onRowDrop, ...rest }: RowProps<T>) => React.JSX.Element;
//# sourceMappingURL=Row.d.ts.map
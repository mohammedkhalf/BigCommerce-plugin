import React, { type RefObject } from 'react';
import type { DiscriminatedTablePaginationProps, TableItem, TableSelectable } from '../types';
export interface ActionsProps<T> {
    customActions?: React.ReactNode;
    forwardedRef: RefObject<HTMLDivElement | null>;
    itemName?: string;
    items: T[];
    pagination?: DiscriminatedTablePaginationProps;
    onSelectionChange?: TableSelectable<T>['onSelectionChange'];
    selectedItems: Set<T>;
    stickyHeader?: boolean;
    tableId: string;
}
export declare const Actions: <T extends TableItem>({ customActions, forwardedRef, itemName, items, onSelectionChange, pagination, selectedItems, stickyHeader, tableId, ...props }: ActionsProps<T>) => React.JSX.Element;
//# sourceMappingURL=Actions.d.ts.map
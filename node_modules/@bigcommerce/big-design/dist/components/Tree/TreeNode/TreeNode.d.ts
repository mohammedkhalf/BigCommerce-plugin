import React from 'react';
import type { TreeNodeProps } from '../types';
interface InternalTreeNodeProps<T> extends TreeNodeProps<T> {
    depth?: number;
    posinset?: number;
    setsize?: number;
    virtualized?: boolean;
    virtualIndex?: number;
    measureElement?: (node: Element | null) => void;
}
export declare const TreeNode: <T>({ children, icon, label, value, id, depth, posinset, setsize, virtualized, virtualIndex, measureElement, }: InternalTreeNodeProps<T>) => React.ReactElement<InternalTreeNodeProps<T>>;
export {};
//# sourceMappingURL=TreeNode.d.ts.map
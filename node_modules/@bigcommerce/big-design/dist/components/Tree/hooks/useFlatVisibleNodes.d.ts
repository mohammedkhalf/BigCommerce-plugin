import type { TreeNodeId, TreeNodeProps } from '../types';
export interface FlatTreeNode<T> {
    node: TreeNodeProps<T>;
    depth: number;
    posinset: number;
    setsize: number;
}
interface UseFlatVisibleNodesProps<T> {
    nodes: Array<TreeNodeProps<T>>;
    expandedNodes: Set<TreeNodeId>;
}
export declare const useFlatVisibleNodes: <T>({ nodes, expandedNodes }: UseFlatVisibleNodesProps<T>) => FlatTreeNode<T>[];
export {};
//# sourceMappingURL=useFlatVisibleNodes.d.ts.map
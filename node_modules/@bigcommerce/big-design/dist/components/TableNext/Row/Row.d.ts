import React, { type ComponentPropsWithoutRef } from 'react';
import type { TableColumn, TableItem, TableSelectable } from '../types';
export interface RowProps<T> extends ComponentPropsWithoutRef<'tr'> {
    columns: Array<TableColumn<T>>;
    index: number;
    isDraggable: boolean;
    isExpanded?: boolean;
    isExpandable?: boolean;
    isHidden?: boolean;
    isParentRow?: boolean;
    isPhantom?: boolean;
    isSelected?: boolean;
    isSelectable?: boolean;
    item: T;
    itemCount: number;
    selectedItems: TableSelectable['selectedItems'];
    showDragIcon?: boolean;
    tableId: string;
    isChildrenRowsSelectable?: TableSelectable['isChildrenRowsSelectable'];
    onExpandedRow?: () => void;
    onItemSelect: () => void;
    onRowDrop?(from: number, to: number): void;
    parentRowId: string;
    childRowId?: string;
    childrenRowsIds: string[];
}
export declare const Row: <T extends TableItem>({ childRowId, childrenRowsIds, columns, index, isChildrenRowsSelectable, isDraggable, isExpandable, isExpanded, isHidden, isParentRow, isPhantom, isSelectable, isSelected, item, itemCount, onExpandedRow, onItemSelect, onRowDrop, parentRowId, selectedItems, showDragIcon, tableId, ...rest }: RowProps<T>) => React.JSX.Element;
//# sourceMappingURL=Row.d.ts.map
import React, { type ComponentPropsWithoutRef, type Ref } from 'react';
import type { MarginProps } from '../../helpers';
import { type BadgeProps } from '../Badge/Badge';
import { type ButtonProps } from '../Button';
interface PrivateProps {
    forwardedRef: Ref<HTMLDivElement>;
}
export interface PanelAction extends Omit<ButtonProps, 'children'> {
    text?: string;
}
export interface PanelProps extends ComponentPropsWithoutRef<'div'>, MarginProps {
    children?: React.ReactNode;
    description?: React.ReactNode;
    header?: string;
    headerId?: string;
    action?: PanelAction;
    badge?: BadgeProps;
}
export declare const RawPanel: React.FC<PanelProps & PrivateProps>;
export declare const Panel: React.ForwardRefExoticComponent<PanelProps & React.RefAttributes<HTMLDivElement>>;
export {};
//# sourceMappingURL=Panel.d.ts.map
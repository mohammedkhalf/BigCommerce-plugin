import { theme as defaultTheme } from '@bigcommerce/big-design-theme';
import styled from 'styled-components';
export function createStyledFlagIcon(FlagIcon) {
  const StyledFlagIcon = /*#__PURE__*/styled(FlagIcon).withConfig({
    displayName: "base__StyledFlagIcon",
    componentId: "sc-1uryeck-0"
  })(["", ""], ({
    size = 'xLarge',
    theme
  }) => ({
    width: typeof size === 'number' ? theme.helpers.remCalc(size) : theme.spacing[size]
  }));
  StyledFlagIcon.defaultProps = {
    theme: defaultTheme
  };
  return StyledFlagIcon;
}
import { isSafari } from '../is-safari';
import { symbols } from './count-events-for-safari';
export function isEnteringWindowInSafari({
  dragEnter
}) {
  if (!isSafari()) {
    return false;
  }
  return dragEnter.hasOwnProperty(symbols.isEnteringWindow);
}